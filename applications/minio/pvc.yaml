apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: minio-data
  namespace: minio
spec:
  accessModes:
    - ReadWriteMany
  storageClassName: minio-static
  volumeName: minio-data-static
  resources:
    requests:
      storage: 20Gi